<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.repairtool">

    <uses-permission android:name="android.permission.INTERNET"/>

    <application
        android:name="com.kahuis.repairtool.RepairApplication"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher_repair"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_repair_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.RepairTool">
        <activity
            android:name="com.kahuis.repairtool.presentation.MainActivity"
            android:exported="true"
            android:theme="@style/Theme.RepairTool.NoActionBar"
            android:windowSoftInputMode="adjustResize">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>